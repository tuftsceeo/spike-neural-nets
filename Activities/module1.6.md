# Shuffling The Data

We can very easily shuffle the data we train our model on with one addition:
```python
DataLoader(dataset, shuffle=True)
```
Why might we want to do this? Make a prediction, then continue and look at the graphs below.
<details>
<summary>Continue</summary>
[Insert a graph here of output values or something over the epoch]
</details>